#!/bin/bash    
sudo docker run -d  ubuntu-gpp bash
sudo docker run -d  ubuntu-gpp bash
sudo docker run -d  ubuntu-gpp bash
sudo docker run -d  ubuntu-gpp bash
sudo docker run -d  ubuntu-gpp bash
sudo docker run -d  ubuntu-gpp bash
sudo docker run -d  ubuntu-gpp bash
sudo docker run -d  ubuntu-gpp bash
sudo docker run -d  ubuntu-gpp bash
sudo docker run -d  ubuntu-gpp bash